# Web framework & API
fastapi
uvicorn
pydantic
python-multipart
streamlit
requests

# Environment
python-dotenv

# Numerical & ML
numpy
torch
sentence-transformers<4
transformers<5
accelerate

# NLP
nltk

# Local PDF parsing
pdfplumber
PyMuPDF

# Optimization
numba
faiss-cpu

# Profiling & benchmarking
psutil
line-profiler

# Visualization & data
matplotlib
pandas
seaborn

# PDF generation (data/generate_corpus.py)
reportlab

# Optional: GPU support (uncomment if GPU available)
# faiss-gpu

# 4-bit quantization (CUDA-only — no Mac wheels exist; runtime falls back
# to float16 if missing). Marker keeps `pip install -r requirements.txt`
# from failing on macOS / non-x86_64 Linux.
bitsandbytes; sys_platform == "linux" and platform_machine == "x86_64"
